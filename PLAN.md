# План нових рубрик

Робочий документ: що додаємо, з чим об'єднуємо і в якому порядку.
Оновлювати разом із `README.md`, коли рубрика виходить у прод.

## Принципи, з яких виходить план

1. **5 постів на день — і не більше.** Було 4 (два з `SCHEDULE` +
   `#ТренажерМозку` + `#ВідеоТижня`), додаємо **третій слот у `SCHEDULE`** —
   денний, приблизно 14:40–15:10, щоб пости розкладалися по дню, а не збивалися
   у вечір: 14:5x → 16:4x → 18:27 → 19:3x → 21:07.
   Завдяки п'ятому посту ротацій потрібно менше: майже кожна нова рубрика
   отримує власний день тижня, а чергуються лише режими в межах однієї теми.
2. **Не більше 3 Gemini-рубрик на день.** Новий денний слот тримаємо під
   безкоштовні рубрики (опитування, дані з API, шаблонні картки). Це головна
   причина, чому п'ятий пост не вигорить квоту: він майже нічого не коштує.
3. **Спершу шукаємо спосіб без Gemini.** `#ТренажерМозку` показав, що
   алгоритмічна рубрика працює і коштує нуль запитів. Опитування, картки з
   даними API, шаблонні тексти — усе це не потребує моделі.
4. **Факти беремо з джерела, не з моделі.** Для історій, рекордів і фактів
   потрібен курований банк тем із перевіреними даними в репо (як
   `data/uk_words.py`), а Gemini лише переказує їх людською мовою. Просити в
   моделі «розкажи невідомий факт» — це замовляти вигадку.
5. **Окремий крон — лише для щоденних безкоштовних рубрик.** Решта живе в
   `SCHEDULE`.
6. **Українська та тон із `EDITORIAL_TEAM_BRIEF`** — усе нове проходить через
   ті самі перевірки (`BANNED_AI_PHRASES`, дедуп тем, модерація).

## Що з чим об'єднуємо

| Хочемо | Куди заходить | Джерело даних | Gemini |
|---|---|---|---|
| Детективний клуб | ✅ вже в `#ТренажерМозку` (алібі, хто бреше, порядок) | код + солвер | 0 |
| Вгадай країну | новий `country`, режим «вгадай» — пн | REST Countries + FlagCDN | 0 |
| Країна в деталях | той самий `country`, режим «профіль» — пн | REST Countries + Вікіпедія UA | 1 |
| Факт дня + Рекорди світу | новий `fact`, два режими — вт | Wikidata + Вікіпедія UA | 0 |
| Нерозгадані історії | новий `stories`, режим «нерозгадане» — ср | банк тем у репо | 1 |
| Історія без нудьги | той самий `stories`, режим «історія» — ср | банк тем у репо | 1 |
| Правда чи фейк | новий `truth_or_fake` — чт | банк тверджень у репо | 0 |
| Кіно та мультфільми | новий `movies` — **щоп'ятниці** | TMDB (uk-UA) + YouTube-трейлер | 0–1 |
| Кіберспорт та Геймінг | новий `esports` — сб | NewsAPI/RSS + фільтр | 1 |
| Тести особистості | новий `personality` — нд | банк тестів у репо | 0 |
| Дивні способи заробітку | режим наявного `side_hustle`, ротація | банк кейсів у репо | 1 |
| Еко-свідомість | режим наявного `cost_of_life`, ротація | банк тем + відкриті дані | 1 |

Сім нових рубрик стають на власний день у денному слоті. Ротація лишається
лише там, де вона природна: режими всередині `country`, `fact` і `stories`
чергуються між собою, а «дивні заробітки» та «еко» — це другий тиждень наявних
`#ПершіГроші` й `#СкількиКоштує`, бо тематично це те саме з іншого боку.

## Крок 0: третій слот і ротація — ✅ зроблено

- `utils/rotation.py` — `next_rubric(slot_key)`: круговий вибір із лічильником у
  Redis, тому перезапуск бота не збиває чергу. Redis лежить — беремо перший
  варіант, слот не втрачаємо.
- `ROTATIONS` у `config.py`; `publish_rubric` розгортає ключ слота, а
  `/test <rubric>` і далі викликає конкретну рубрику напряму.
- **Резервна рубрика** `RUBRIC_FALLBACK`: коли генератор віддав порожньо або
  вигоріла квота, слот закриває безкоштовна рубрика. Один рівень, без рекурсії.
  Поки що порожнє значення — увімкнемо, коли з'явиться `fact`.
- Денний слот поки лише в понеділок (під `country`). Кожна наступна рубрика
  додає слот свого дня разом із собою — інакше в розкладі стоятимуть дні, де
  публікувати нічого.

## Хвиля 1: без Gemini або майже

Найкраще співвідношення «користь / ризик». Усе тримається на відкритих API без
ключів або на банку даних у репо.

1. ✅ **`country` — «Вгадай країну» + «Країна в деталях».** Готово.
   Режим «вгадай»: прапор картинкою, опитування — нуль Gemini, конвеєр квізу
   перевикористали цілком (як для `brain`), розв'язок наступного дня.
   Режим «деталі»: цифри з Wikidata + абзац Вікіпедії UA, Gemini переказує.
   Дедуп країн у Redis окремо на кожен режим.
   **Джерело змінилося на ходу:** REST Countries із 2026-го вимкнув v1–v4, а v5
   потребує `Authorization`, тому дані беремо з Wikidata (SPARQL). Українські
   назви країн, столиць і валют вона віддає сама — 196 країн, ключ не потрібен.
   Мінус — запит триває десятки секунд, тому довідник кешуємо на два тижні.
   Прапори — FlagCDN, теж без ключа.
   Слот: **пн, денний**, режими чергуються через тиждень.
2. **`fact` — «Факт дня» + «Рекорди світу».**
   Wikidata SPARQL і Вікіпедія UA, шаблонний текст без моделі. Обов'язково
   кешувати запити й тримати банк тем, бо SPARQL повільний і крихкий.
   Слот: **вт, денний** + резервна рубрика для порожніх слотів.
3. **`personality` — тести особистості.**
   Telegram-опитування дає одну відповідь, тому це одне питання з 4 варіантами,
   а наступного дня — розбір усіх типів зі статистикою каналу (той самий крон
   `QUIZ_ANSWER_CRON_TIME`). Банк тестів статичний, у репо — нуль Gemini і нуль
   ризику вигадок.
   Слот: **нд, денний**.
4. **`esports` — Кіберспорт та Геймінг.**
   Готового безкоштовного API немає, тому NewsAPI + RSS із фільтром за
   ключовими словами, як зроблено для банкнот. Акцент на грошах і карʼєрі в
   кіберспорті — так рубрика не випадає з позиціонування каналу.
   Слот: **сб, денний**.
5. **`weird_income` — Дивні способи заробітку.**
   Режим наявного `side_hustle`: банк реальних кейсів у репо, Gemini робить із
   кейсу пост. Обов'язкова рамка «чи це законно і скільки насправді платять» —
   інакше рубрика перетворюється на рекламу схем.
   Слот: ср 16:53, через тиждень із `side_hustle`.

## Хвиля 2: потрібен новий API-ключ

6. **`movies` — Що подивитися ввечері, щоп'ятниці.**
   TMDB: `/discover/movie` з `language=uk-UA`, жанри 10751 (сімейне) і 16
   (анімація). Трейлер — із `/movie/{id}/videos` (ключ YouTube), постер — із
   TMDB. Опис українською часто вже є в API, тож Gemini або не потрібен, або
   дає одне речення «кому це зайде».
   **Свіжість — головний фільтр.** Беремо тільки те, що вийшло за останні ~30
   днів або виходить у найближчі ~30 (`primary_release_date.gte/lte`), і
   відкидаємо фільми без трейлера чи без постера. Дедуп по `movie_id` у Redis,
   щоб той самий реліз не прилітав двічі. Якщо свіжого сімейного релізу тижня
   немає — рубрика чесно повертає `None`, а слот закриває резервна рубрика.
   Вік: українських сертифікатів у TMDB немає, тому `certification_country=US`
   і `certification.lte=PG`.
   Слот: **пт, денний, щотижня.** Наявні п'ятничні рубрики нікуди не
   переїжджають — для кіно є новий слот.
   Потрібно: `TMDB_API_KEY` у `.env`, згадка TMDB у README (умови атрибуції).

## Хвиля 3: текстові рубрики, найбільший ризик якості

Тут Gemini пише багато, тому банк тем із фактами обов'язковий. Робити після
того, як хвилі 1–2 стабільно працюють.

7. **`stories` — Нерозгадані історії + Історія без нудьги.**
   Банк тем у репо: назва, епоха, 3–5 перевірених фактів, чим саме історія
   цікава. Gemini збирає з цього розповідь і не додає «фактів» від себе.
   Слот: **ср, денний**, режими чергуються через тиждень.
8. **`truth_or_fake` — Правда чи фейк.**
   Банк тверджень із поясненнями, публікація як опитування. Частково вже є:
   `poll_options` і «Який факт — брехня шахрая?» у `publisher`.
   Слот: **чт, денний**.
9. **`eco` — Еко-свідомість.**
   Кут, який тримає тему в рамках каналу: скільки еко-звички коштують або
   економлять у гривнях. Тоді це продовження `#СкількиКоштує`, а не окрема
   проповідь.
   Слот: вт 17:14, через тиждень із `cost_of_life`.

## Як виглядатиме тиждень після всіх хвиль

| День | Денний (новий) | Слот 2 | Слот 3 |
|---|---|---|---|
| Пн | `country` (вгадай ⇄ деталі) | `ai_news` | `game_economy` |
| Вт | `fact` (факт ⇄ рекорд) | `cost_of_life` ⇄ `eco` | `ai_hack` |
| Ср | `stories` (нерозгадане ⇄ історія) | `side_hustle` ⇄ `weird_income` | `crime` |
| Чт | `truth_or_fake` | `crypto` | `behavioral_finance` |
| Пт | `movies` | `subscription_trap` | `quiz` |
| Сб | `esports` | `careers` | `startup_week` |
| Нд | `personality` | `money_myth` | `quiz` |

Плюс щодня `#ТренажерМозку` і `#ВідеоТижня`, плюс подієві `#НоваБанкнота`.
Разом 5 постів на день. Gemini-рубрик на день: 2–3, бо денний слот майже
завжди безкоштовний (виняток — `stories` і `esports`).

## Чек-лист на кожну нову рубрику

- Генератор у `generators/`, ключ у `GENERATORS`, хештег і персона.
- Дедуп у Redis: не повторювати тему/країну/фільм/історію.
- Опитувальні рубрики — через `POLL_QUIZ_RUBRICS`; довга умова йде в підпис,
  а в опитування — короткий `poll_question` (ліміт Telegram 300 символів).
- Тести: генератор віддає валідний пост, дедуп працює, ліміти Telegram
  дотримані, зовнішній API замокано.
- `README.md`, `env.example`, `/test <rubric>`.

## Ухвалені рішення

- **5 постів на день**, третій слот у `SCHEDULE` — денний.
- **Кіно щоп'ятниці**, головний критерій — свіжість релізу; наявні п'ятничні
  рубрики не переїжджають.
- **TMDB-ключ буде**, рубрику робимо на TMDB.
- Починаємо з кроку 0 і рубрики `country`.

## Ще не вирішено

- Чи потрібен банк тем українською під історії, чи допускаємо англомовні
  джерела з перекладом на льоту.
- Чи публікувати «Вгадай країну» і «Тест особистості» з розв'язком наступного
  дня (як квіз) або того ж вечора.
